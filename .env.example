# Supabase (create project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Required only for scripts/create-example-user.ts (do not expose in client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Canvas LMS API (token from Canvas Profile → Settings → New Access Token)
CANVAS_API_URL=https://your-school.instructure.com
CANVAS_ACCESS_TOKEN=your-canvas-access-token

# MiniMax API (for StudyBuddy AI - get key from platform.minimax.io)
MINIMAX_API_KEY=your-minimax-api-key

# Stability AI (Stable Diffusion) - for avatar style transfer. Get key at platform.stability.ai
# If set, style transfer uses this diffusion model instead of MiniMax.
STABILITY_API_KEY=your-stability-api-key
