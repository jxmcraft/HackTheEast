# Supabase (create project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Required only for scripts/create-example-user.ts (do not expose in client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Embeddings (required for sync & semantic search) ---
# Default: local in-process model (no API keys, no separate server). Model loads on first use when you run the app.
# Uses all-MiniLM-L6-v2 (384 dimensions). Run: supabase/migrations/20250228100010_embedding_384_local.sql
#
# Optional: use OpenAI — set EMBEDDING_PROVIDER=openai and OPENAI_EMBEDDING_API_KEY (then use vector(1536) or a 768-d OpenAI model).
# Optional: use MiniMax — set EMBEDDING_PROVIDER=minimax and MINIMAX_API_KEY, MINIMAX_GROUP_ID (requires vector(1536)).
EMBEDDING_PROVIDER=
# Examples: leave empty or "local" = local model (default). "openai" = OpenAI. "minimax" = MiniMax.

# --- MiniMax (only if EMBEDDING_PROVIDER=minimax) ---
# MINIMAX_API_KEY=
# MINIMAX_GROUP_ID=

# --- MiniMax Chat + TTS (audio-visual: script, slides, podcast audio) ---
# Use api.minimax.io only (never .chat). Chat: no Group-Id. TTS: api-uw.minimax.io.
# M2-her: max 2048 output tokens. For longer scripts use a Text Generation model (same endpoint):
#   MINIMAX_CHAT_MODEL=MiniMax-M2.5  (supports up to 10240 tokens)
# MINIMAX_CHAT_MODEL=M2-her
# MINIMAX_TTS_VOICE=English_expressive_narrator

# --- ElevenLabs (optional; not used for podcast – podcast uses MiniMax TTS) ---
# ELEVENLABS_API_KEY=
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# --- Featherless (slide deck mode only: slide background images) ---
# FEATHERLESS_API_KEY=
# FEATHERLESS_API_BASE=https://api.featherless.ai
# FEATHERLESS_IMAGE_MODEL=stabilityai/stable-diffusion-xl-base-1.0

# --- Supabase Storage (audio-visual lessons) ---
# Create a bucket named "lesson-audio" in Supabase Storage and set public or RLS as needed.

# --- OpenAI embeddings (only if EMBEDDING_PROVIDER=openai) ---
# OPENAI_EMBEDDING_API_KEY=
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_API_BASE=

# --- Chat / LLM (for lesson generation etc.) ---
# Featherless (works in Hong Kong): https://featherless.ai — set FEATHERLESS_API_KEY.
# Or OpenAI: OPENAI_API_KEY, optionally OPENAI_API_BASE.
OPENAI_API_KEY=
FEATHERLESS_API_KEY=
# FEATHERLESS_CHAT_MODEL=Qwen/Qwen2.5-7B-Instruct
